<!-- DE ProActive: Premium Plans + Included Features (Cornerstone Raw Content) -->
<section class="de-proactive-v2" aria-label="ProActive Ecosystem Plans and Included Features">
  <div class="de-proactive-surface">
    <header class="de-head">
      <span class="de-eyebrow">Included in every plan</span>
      <h2 class="de-title">ProActive Ecosystem Foundation</h2>
      <p class="de-sub">
        Office, Business, and Enterprise all include the same managed foundation. Higher tiers add depth:
        Basic IT → Security → Compliance.
      </p>
    </header>

    <!-- Plans -->
    <div class="de-plans" role="list">
      <article class="de-plan de-plan--office" role="listitem">
        <div class="de-plan-top">
          <div class="de-plan-badge">Office</div>
          <div class="de-plan-name">Basic IT</div>
        </div>
        <div class="de-plan-price">
          <span class="de-amt">$129</span>
          <span class="de-term">/ user / month</span>
        </div>
        <div class="de-plan-note">A clean, managed IT baseline.</div>
      </article>

      <article class="de-plan de-plan--business" role="listitem">
        <div class="de-plan-top">
          <div class="de-plan-badge">Business</div>
          <div class="de-plan-name">Security</div>
        </div>
        <div class="de-plan-price">
          <span class="de-amt">$189</span>
          <span class="de-term">/ user / month</span>
        </div>
        <div class="de-plan-note">Adds stronger protection and response.</div>
      </article>

      <article class="de-plan de-plan--enterprise" role="listitem">
        <div class="de-plan-top">
          <div class="de-plan-badge">Enterprise</div>
          <div class="de-plan-name">Compliance</div>
        </div>
        <div class="de-plan-price">
          <span class="de-amt">$249</span>
          <span class="de-term">/ user / month</span>
        </div>
        <div class="de-plan-note">Adds governance and audit readiness.</div>
      </article>
    </div>

    <!-- Features -->
    <div class="de-features">
      <article class="de-card">
        <div class="de-ic">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2a7 7 0 0 0-7 7v3.2c0 .6.3 1.1.8 1.4l6 3.4c.7.4 1.6.4 2.3 0l6-3.4c.5-.3.8-.8.8-1.4V9a7 7 0 0 0-7-7Zm0 2a5 5 0 0 1 5 5v3.1l-5.8 3.3a.7.7 0 0 1-.4 0L7 12.1V9a5 5 0 0 1 5-5Z"/></svg>
        </div>
        <h3 class="de-card-title">Service Desk &amp; Support</h3>
        <p class="de-card-text">Fast help for day-to-day issues, questions, and requests—plus escalation when it’s more complex.</p>
      </article>

      <article class="de-card de-card--wide">
        <div class="de-ic">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 3a9 9 0 1 0 9 9h-2a7 7 0 1 1-7-7V3Zm1 5h8v2h-8V8Zm0 4h6v2h-6v-2Z"/></svg>
        </div>
        <h3 class="de-card-title">Managed Network Security &amp; Connectivity</h3>
        <p class="de-card-text">
          Works with your current network or replaces it when needed. We review router/firewall, Wi-Fi, and switches,
          identify support/end-of-life risks, and provide a recommended timeline—plus ongoing monitoring, updates,
          and security settings management.
        </p>
      </article>

      <article class="de-card">
        <div class="de-ic">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M7 2h10a2 2 0 0 1 2 2v16a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2Zm0 2v16h10V4H7Zm2 13h6v2H9v-2Z"/></svg>
        </div>
        <h3 class="de-card-title">Device &amp; User Management</h3>
        <p class="de-card-text">We manage users, devices, access, and standard configurations to keep everything consistent.</p>
      </article>

      <article class="de-card">
        <div class="de-ic">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M4 20V4h2v14h14v2H4Zm4-4 3-4 3 2 4-6 2 1-5 7-3-2-3 4-1-2Z"/></svg>
        </div>
        <h3 class="de-card-title">Monitoring &amp; Maintenance</h3>
        <p class="de-card-text">Always-on monitoring and proactive maintenance to reduce downtime and surprises.</p>
      </article>

      <article class="de-card">
        <div class="de-ic">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2a10 10 0 1 0 10 10h-2A8 8 0 1 1 12 4V2Zm1 5h-2v6l5 3 1-1.7-4-2.3V7Z"/></svg>
        </div>
        <h3 class="de-card-title">Updates &amp; Patch Management</h3>
        <p class="de-card-text">Operating systems and core apps kept current to reduce security and stability risks.</p>
      </article>

      <article class="de-card">
        <div class="de-ic">
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 1 3 5v6c0 5.6 3.8 10.8 9 12 5.2-1.2 9-6.4 9-12V5l-9-4Zm0 4.2L19 8v3c0 4.4-2.8 8.6-7 9.9C7.8 19.6 5 15.4 5 11V8l7-2.8ZM11 7h2v7h-2V7Zm0 9h2v2h-2v-2Z"/></svg>
        </div>
        <h3 class="de-card-title">Security Settings Management</h3>
        <p class="de-card-text">Baseline hardening and secure configuration management, maintained consistently over time.</p>
      </article>
    </div>
  </div>
</section>

<style>
  /* ===== DE ProActive v2 (scoped) ===== */
  .de-proactive-v2{
    --de-cyan:#27caf2;
    --de-blue:#0056f5;
    --de-purple:#b300ff;
    --de-magenta:#ce0fb5;
    --de-white:#ffffff;
    --de-ink:#070813;

    --de-surface: rgba(255,255,255,.06);
    --de-surface2: rgba(255,255,255,.035);
    --de-border: rgba(255,255,255,.14);
    --de-text: rgba(255,255,255,.92);
    --de-muted: rgba(255,255,255,.72);
    --de-shadow: 0 18px 60px rgba(0,0,0,.35);

    max-width: 1180px;
    margin: 0 auto;
  }

  /* Section “own background” so it looks good even if page is plain */
  .de-proactive-surface{
    border-radius: 26px;
    padding: 28px;
    border: 1px solid rgba(255,255,255,.12);
    background:
      radial-gradient(900px 420px at 12% 8%, rgba(39,202,242,.18), transparent 55%),
      radial-gradient(900px 420px at 88% 18%, rgba(179,0,255,.16), transparent 55%),
      radial-gradient(900px 420px at 70% 92%, rgba(0,86,245,.14), transparent 55%),
      linear-gradient(135deg, rgba(7,8,19,.92), rgba(7,8,19,.70));
    box-shadow: var(--de-shadow);
    position: relative;
    overflow: hidden;
  }

  /* Subtle “glow edge” */
  .de-proactive-surface:before{
    content:"";
    position:absolute; inset:-2px;
    background: linear-gradient(90deg, rgba(39,202,242,.45), rgba(0,86,245,.35), rgba(179,0,255,.40), rgba(206,15,181,.35));
    filter: blur(18px);
    opacity: .35;
    pointer-events:none;
  }
  .de-proactive-surface > *{ position: relative; }

  .de-head{ margin-bottom: 18px; }
  .de-eyebrow{
    display:inline-flex;
    padding: 6px 10px;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,.18);
    background: rgba(255,255,255,.06);
    color: var(--de-muted);
    font-size: 12px;
    letter-spacing: .08em;
    text-transform: uppercase;
  }
  .de-title{
    margin: 10px 0 8px 0;
    color: var(--de-text);
    line-height: 1.1;
    font-weight: 800;
    letter-spacing: -0.03em;
    font-size: clamp(26px, 3.2vw, 40px);
  }
  .de-sub{
    margin: 0;
    color: var(--de-muted);
    line-height: 1.6;
    max-width: 78ch;
    font-size: 15px;
  }

  /* Plans */
  .de-plans{
    display:grid;
    grid-template-columns: 1fr;
    gap: 14px;
    margin: 18px 0 18px;
  }
  .de-plan{
    border-radius: 18px;
    padding: 16px 16px 14px;
    border: 1px solid var(--de-border);
    background: linear-gradient(180deg, rgba(255,255,255,.07), rgba(255,255,255,.03));
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    position: relative;
    overflow: hidden;
    transform: translateZ(0);
  }
  .de-plan:after{
    content:"";
    position:absolute; left:0; right:0; top:0; height: 4px;
    background: linear-gradient(90deg, var(--de-cyan), var(--de-blue), var(--de-purple), var(--de-magenta));
    opacity: .9;
  }
  .de-plan--office:after{ background: linear-gradient(90deg, var(--de-cyan), var(--de-blue)); }
  .de-plan--business:after{ background: linear-gradient(90deg, var(--de-blue), var(--de-purple)); }
  .de-plan--enterprise:after{ background: linear-gradient(90deg, var(--de-purple), var(--de-magenta)); }

  .de-plan-top{
    display:flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-top: 4px;
  }
  .de-plan-badge{
    display:inline-flex;
    padding: 6px 10px;
    border-radius: 999px;
    background: rgba(255,255,255,.07);
    border: 1px solid rgba(255,255,255,.14);
    color: var(--de-text);
    font-size: 12px;
    font-weight: 700;
  }
  .de-plan-name{
    color: var(--de-text);
    font-weight: 800;
    letter-spacing: -0.02em;
    font-size: 16px;
    text-align: right;
  }
  .de-plan-price{
    margin-top: 12px;
    display:flex;
    align-items: baseline;
    gap: 8px;
  }
  .de-amt{
    color: var(--de-text);
    font-weight: 900;
    font-size: 30px;
    letter-spacing: -0.03em;
  }
  .de-term{
    color: var(--de-muted);
    font-size: 13px;
  }
  .de-plan-note{
    margin-top: 8px;
    color: var(--de-muted);
    font-size: 13px;
    line-height: 1.45;
  }

  /* Features */
  .de-features{
    display:grid;
    grid-template-columns: 1fr;
    gap: 14px;
  }
  .de-card{
    border-radius: 18px;
    padding: 16px;
    border: 1px solid var(--de-border);
    background:
      radial-gradient(400px 180px at 20% 0%, rgba(39,202,242,.10), transparent 55%),
      linear-gradient(180deg, rgba(255,255,255,.06), rgba(255,255,255,.03));
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    position: relative;
    overflow: hidden;
    transition: transform .18s ease, border-color .18s ease, box-shadow .18s ease;
  }
  .de-ic{
    width: 38px;
    height: 38px;
    border-radius: 12px;
    display:grid;
    place-items:center;
    border: 1px solid rgba(255,255,255,.16);
    background: linear-gradient(135deg, rgba(39,202,242,.22), rgba(179,0,255,.18));
    margin-bottom: 10px;
  }
  .de-ic svg{
    width: 20px; height: 20px;
    fill: rgba(255,255,255,.90);
  }
  .de-card-title{
    margin: 0 0 8px 0;
    color: var(--de-text);
    font-weight: 850;
    letter-spacing: -0.02em;
    font-size: 16px;
    line-height: 1.25;
  }
  .de-card-text{
    margin: 0;
    color: var(--de-muted);
    font-size: 14px;
    line-height: 1.6;
  }

  /* Hover polish (desktop) */
  @media (hover:hover){
    .de-plan:hover, .de-card:hover{
      transform: translateY(-2px);
      border-color: rgba(255,255,255,.22);
      box-shadow: 0 18px 45px rgba(0,0,0,.28);
    }
  }

  /* Responsive grids */
  @media (min-width: 760px){
    .de-plans{ grid-template-columns: repeat(3, 1fr); }
    .de-features{ grid-template-columns: repeat(2, 1fr); }
    .de-card--wide{ grid-column: span 2; }
  }
  @media (min-width: 1080px){
    .de-features{ grid-template-columns: repeat(3, 1fr); }
    .de-card--wide{ grid-column: span 1; } /* keep balanced on large screens */
  }

  /* Motion accessibility */
  @media (prefers-reduced-motion: reduce){
    .de-plan, .de-card{ transition: none !important; }
  }
</style>
