/* =========================================================
   DE Sales Process v4 (scoped / Cornerstone-safe)
   Scope: #de-sales-process.de-sp4
   ========================================================= */

#de-sales-process.de-sp4{
  --sp-ink: rgba(255,255,255,.96);
  --sp-ink2: rgba(255,255,255,.82);
  --sp-ink3: rgba(255,255,255,.62);
  --sp-stroke: rgba(255,255,255,.16);
  --sp-glass1: rgba(255,255,255,.08);
  --sp-glass2: rgba(255,255,255,.03);
  --sp-accent: #ffb800;
  --sp-blue: #4a9eff;
  --sp-green: #34d399;
  --sp-r: 20px;
  --sp-ease: cubic-bezier(.2,.8,.2,1);

  padding: 22px 0 10px;
}

#de-sales-process.de-sp4 .de-sp4-wrap{
  max-width: 1800px;
  margin: 0 auto;
  padding: 0 16px;
}

/* Header */
#de-sales-process.de-sp4 .de-sp4-header{
  text-align: center;
  padding: 18px 10px 10px;
}

#de-sales-process.de-sp4 .de-sp4-kicker{
  display:inline-block;
  letter-spacing:.18em;
  text-transform:uppercase;
  font-weight: 900;
  font-size: 11px;
  color: var(--sp-accent);
  background: rgba(255,184,0,.12);
  padding: 8px 16px;
  border-radius: 999px;
  border: 1px solid rgba(255,184,0,.3);
  margin-bottom: 16px;
}

#de-sales-process.de-sp4 .de-sp4-title{
  font-weight: 900;
  font-size: clamp(34px, 5vw, 56px);
  letter-spacing: -.03em;
  background: linear-gradient(135deg, #fff 0%, #ffb800 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  margin: 10px 0 10px;
  text-shadow: 0 0 80px rgba(255,184,0,.25);
}

#de-sales-process.de-sp4 .de-sp4-sub{
  max-width: 820px;
  margin: 0 auto 16px;
  color: var(--sp-ink2);
  font-size: 16px;
  line-height: 1.6;
  font-weight: 550;
}

/* Stats */
#de-sales-process.de-sp4 .de-sp4-stats{
  display:flex;
  gap: 14px;
  justify-content:center;
  flex-wrap: wrap;
  margin: 18px 0 18px;
}

#de-sales-process.de-sp4 .stat-item{
  background: linear-gradient(135deg, rgba(255,255,255,.10), rgba(255,255,255,.03));
  border: 1px solid var(--sp-stroke);
  border-radius: 16px;
  padding: 16px 22px;
  backdrop-filter: blur(10px);
  transition: transform .3s var(--sp-ease), border-color .3s var(--sp-ease), box-shadow .3s var(--sp-ease);
}

#de-sales-process.de-sp4 .stat-item:hover{
  transform: translateY(-3px);
  border-color: rgba(255,184,0,.4);
  box-shadow: 0 20px 60px rgba(255,184,0,.16);
}

#de-sales-process.de-sp4 .stat-value{
  font-size: 28px;
  font-weight: 900;
  color: var(--sp-accent);
  display:block;
}
#de-sales-process.de-sp4 .stat-label{
  font-size: 12px;
  color: var(--sp-ink3);
  text-transform: uppercase;
  letter-spacing: .1em;
  font-weight: 750;
}

/* Controls */
#de-sales-process.de-sp4 .de-sp4-controls{
  display:flex;
  justify-content:center;
  margin: 12px 0 0;
}
#de-sales-process.de-sp4 .de-sp4-search{
  width: min(520px, 100%);
  padding: 14px 18px;
  border-radius: 999px;
  border: 1px solid var(--sp-stroke);
  background: rgba(0,0,0,.28);
  color: var(--sp-ink);
  font-size: 15px;
  font-weight: 550;
  transition: box-shadow .3s var(--sp-ease), border-color .3s var(--sp-ease);
}
#de-sales-process.de-sp4 .de-sp4-search:focus{
  outline: none;
  border-color: var(--sp-accent);
  box-shadow: 0 0 0 3px rgba(255,184,0,.18);
}
#de-sales-process.de-sp4 .de-sp4-search::placeholder{ color: var(--sp-ink3); }

/* Tabs */
#de-sales-process.de-sp4 .de-sp4-tabs{
  display:flex;
  gap: 14px;
  justify-content:center;
  margin: 18px 0 10px;
  flex-wrap: wrap;
}
#de-sales-process.de-sp4 .de-sp4-tab{
  border: 1px solid var(--sp-stroke);
  background: rgba(0,0,0,.36);
  backdrop-filter: blur(10px);
  color: var(--sp-ink2);
  padding: 14px 26px;
  border-radius: 999px;
  font-weight: 850;
  font-size: 14px;
  cursor:pointer;
  transition: transform .3s var(--sp-ease), border-color .3s var(--sp-ease), background .3s var(--sp-ease);
}
#de-sales-process.de-sp4 .de-sp4-tab:hover{
  transform: translateY(-2px);
  border-color: rgba(255,184,0,.4);
}
#de-sales-process.de-sp4 .de-sp4-tab.is-active{
  border-color: var(--sp-accent);
  background: linear-gradient(135deg, rgba(255,184,0,.20), rgba(255,184,0,.05));
  color: var(--sp-ink);
  box-shadow: 0 8px 32px rgba(255,184,0,.18), inset 0 1px 0 rgba(255,255,255,.08);
}

/* Progress */
#de-sales-process.de-sp4 .de-sp4-progress{
  margin: 14px auto 0;
  padding: 14px;
  background: rgba(0,0,0,.24);
  border-radius: 16px;
  border: 1px solid var(--sp-stroke);
  max-width: 720px;
}
#de-sales-process.de-sp4 .progress-label{
  font-size: 12px;
  color: var(--sp-ink3);
  margin-bottom: 10px;
  font-weight: 750;
  text-transform: uppercase;
  letter-spacing: .1em;
}
#de-sales-process.de-sp4 .progress-bar{
  height: 8px;
  background: rgba(255,255,255,.10);
  border-radius: 999px;
  overflow:hidden;
}
#de-sales-process.de-sp4 .progress-fill{
  height: 100%;
  width: 0;
  background: linear-gradient(90deg, var(--sp-accent), var(--sp-blue));
  border-radius: 999px;
  transition: width .6s var(--sp-ease);
}

/* Rows */
#de-sales-process.de-sp4 .de-sp4-row{ margin: 42px 0 0; }
#de-sales-process.de-sp4 .de-sp4-rowhead{ margin: 0 0 16px; padding: 0 6px; }
#de-sales-process.de-sp4 .de-sp4-rowkicker{
  display:inline-block;
  text-transform: uppercase;
  letter-spacing: .15em;
  font-weight: 850;
  font-size: 11px;
  color: var(--sp-accent);
  margin-bottom: 8px;
}
#de-sales-process.de-sp4 .de-sp4-rowtitle{
  font-size: 20px;
  color: var(--sp-ink);
  font-weight: 850;
  line-height: 1.35;
}

/* Timeline */
#de-sales-process.de-sp4 .de-sp4-timeline{
  position: relative;
  display:flex;
  gap: 18px;
  overflow-x:auto;
  padding: 34px 6px 22px;
  scroll-snap-type: x mandatory;
  -webkit-overflow-scrolling: touch;
}
#de-sales-process.de-sp4 .de-sp4-timeline::before{
  content:"";
  position:absolute;
  left: 18px; right: 18px;
  top: 22px;
  height: 3px;
  background: linear-gradient(90deg, var(--sp-accent), var(--sp-blue), var(--sp-green));
  border-radius: 999px;
  opacity: .22;
}
#de-sales-process.de-sp4 .de-sp4-timeline::-webkit-scrollbar{ height: 8px; }
#de-sales-process.de-sp4 .de-sp4-timeline::-webkit-scrollbar-track{ background: rgba(255,255,255,.06); border-radius: 999px; }
#de-sales-process.de-sp4 .de-sp4-timeline::-webkit-scrollbar-thumb{
  background: linear-gradient(90deg, var(--sp-accent), var(--sp-blue));
  border-radius: 999px;
}

/* Cards */
#de-sales-process.de-sp4 .de-sp4-card{
  scroll-snap-align: start;
  min-width: 360px;
  max-width: 390px;
  border-radius: var(--sp-r);
  border: 1px solid var(--sp-stroke);
  background: linear-gradient(135deg, rgba(255,255,255,.12), rgba(255,255,255,.04));
  backdrop-filter: blur(18px);
  box-shadow: 0 20px 60px rgba(0,0,0,.40);
  padding: 22px;
  position: relative;
  transition: transform .35s var(--sp-ease), border-color .35s var(--sp-ease), box-shadow .35s var(--sp-ease), background .35s var(--sp-ease);
  overflow:hidden;
}
#de-sales-process.de-sp4 .de-sp4-card:hover{
  transform: translateY(-6px) scale(1.01);
  border-color: rgba(255,184,0,.45);
  box-shadow: 0 30px 90px rgba(255,184,0,.20);
}
#de-sales-process.de-sp4 .de-sp4-card.is-active{
  border-color: var(--sp-accent);
  background: linear-gradient(135deg, rgba(255,184,0,.15), rgba(255,184,0,.05));
  box-shadow: 0 30px 90px rgba(255,184,0,.26);
  transform: translateY(-3px) scale(1.01);
}

/* Card top */
#de-sales-process.de-sp4 .de-sp4-cardtop{
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap: 12px;
  margin-bottom: 14px;
}
#de-sales-process.de-sp4 .de-sp4-cardtitle{
  font-weight: 900;
  font-size: 16px;
  color: var(--sp-ink);
  line-height: 1.3;
}
#de-sales-process.de-sp4 .de-sp4-badge{
  border: 1px solid rgba(255,184,0,.30);
  background: rgba(255,184,0,.12);
  color: var(--sp-accent);
  padding: 8px 14px;
  border-radius: 999px;
  font-weight: 900;
  font-size: 12px;
  letter-spacing: .05em;
}

/* List */
#de-sales-process.de-sp4 .de-sp4-list{
  margin: 0 0 14px;
  padding-left: 20px;
  color: var(--sp-ink2);
  font-weight: 520;
  line-height: 1.6;
  font-size: 14px;
}
#de-sales-process.de-sp4 .de-sp4-list li{ margin-bottom: 8px; }
#de-sales-process.de-sp4 .de-sp4-list li::marker{ color: var(--sp-accent); }

/* Meta */
#de-sales-process.de-sp4 .de-sp4-meta{
  display:grid;
  gap: 12px;
  margin: 14px 0;
}
#de-sales-process.de-sp4 .de-sp4-mline{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap: 12px;
  padding: 12px 14px;
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,.15);
  background: rgba(0,0,0,.26);
  color: var(--sp-ink2);
  font-weight: 600;
  font-size: 14px;
}
#de-sales-process.de-sp4 .de-sp4-mline b{
  color: var(--sp-accent);
  font-weight: 850;
}

/* Details button */
#de-sales-process.de-sp4 .de-sp4-more{
  width: 100%;
  border-radius: 14px;
  border: 1px solid rgba(255,255,255,.15);
  background: rgba(0,0,0,.28);
  color: var(--sp-ink);
  font-weight: 850;
  padding: 13px 16px;
  cursor:pointer;
  transition: transform .25s var(--sp-ease), border-color .25s var(--sp-ease), background .25s var(--sp-ease);
}
#de-sales-process.de-sp4 .de-sp4-more:hover{
  transform: translateY(-2px);
  border-color: var(--sp-accent);
  background: rgba(255,184,0,.08);
}

/* Drawer */
#de-sales-process.de-sp4 .de-sp4-drawer{
  margin-top: 24px;
  border-radius: var(--sp-r);
  border: 1px solid rgba(255,184,0,.28);
  background: linear-gradient(135deg, rgba(255,184,0,.15), rgba(255,184,0,.05));
  backdrop-filter: blur(18px);
  box-shadow: 0 30px 90px rgba(255,184,0,.20);
  padding: 22px;
}
#de-sales-process.de-sp4 .de-sp4-drawer-top{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap: 16px;
  margin-bottom: 14px;
}
#de-sales-process.de-sp4 .de-sp4-drawer-tag{
  display:inline-flex;
  padding: 10px 14px;
  border-radius: 999px;
  border: 1px solid rgba(255,184,0,.38);
  background: rgba(255,184,0,.12);
  color: var(--sp-ink);
  font-weight: 850;
  font-size: 12px;
  letter-spacing: .08em;
  text-transform: uppercase;
}
#de-sales-process.de-sp4 .de-sp4-drawer-x{
  width: 40px; height: 40px;
  border-radius: 12px;
  border: 1px solid rgba(255,255,255,.20);
  background: rgba(0,0,0,.28);
  color: var(--sp-ink);
  font-size: 22px;
  font-weight: 700;
  cursor:pointer;
}
#de-sales-process.de-sp4 .de-sp4-drawer-title{
  margin: 0 0 12px;
  color: var(--sp-ink);
  font-weight: 900;
  font-size: 22px;
}
#de-sales-process.de-sp4 .de-sp4-drawer-body{
  color: var(--sp-ink2);
  font-weight: 520;
  line-height: 1.7;
  font-size: 15px;
}
#de-sales-process.de-sp4 .de-sp4-drawer-body ul{ padding-left: 24px; margin: 10px 0; }

/* Panels */
#de-sales-process.de-sp4 .de-sp4-panel[hidden]{ display:none !important; }

/* Responsive */
@media (max-width: 860px){
  #de-sales-process.de-sp4 .de-sp4-card{ min-width: 85vw; max-width: 85vw; }
  #de-sales-process.de-sp4 .de-sp4-tabs{ flex-direction: column; }
  #de-sales-process.de-sp4 .de-sp4-tab{ width: 100%; }
}
