<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>USP Development Worksheet - Digerati Experts</title>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #FFD700;
            --dark: #0A0E1A;
            --darker: #050810;
            --accent: #1E90FF;
            --text: #E8E8E8;
            --text-dim: #A0A0A0;
            --border: #2A2F3F;
            --input-bg: #1A1F2E;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'DM Sans', sans-serif;
            background: var(--darker);
            color: var(--text);
            line-height: 1.6;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 40px 24px;
        }

        /* Header */
        .header {
            border-bottom: 3px solid var(--primary);
            padding-bottom: 30px;
            margin-bottom: 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo-section {
            flex: 1;
        }

        .logo {
            font-family: 'Space Mono', monospace;
            font-size: 24px;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 8px;
            letter-spacing: -0.5px;
        }

        .tagline {
            font-size: 12px;
            color: var(--text-dim);
            text-transform: uppercase;
            letter-spacing: 2px;
            font-weight: 500;
        }

        .action-buttons {
            display: flex;
            gap: 12px;
        }

        .btn {
            padding: 12px 24px;
            border: 2px solid var(--accent);
            background: transparent;
            color: var(--accent);
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'DM Sans', sans-serif;
            border-radius: 4px;
        }

        .btn:hover {
            background: var(--accent);
            color: var(--dark);
            transform: translateY(-2px);
        }

        .btn-primary {
            background: var(--accent);
            color: var(--dark);
        }

        .btn-primary:hover {
            background: var(--primary);
            border-color: var(--primary);
        }

        /* Main Title */
        .main-title {
            font-size: 36px;
            font-weight: 700;
            line-height: 1.2;
            margin-bottom: 20px;
            color: var(--primary);
            text-align: center;
        }

        .subtitle {
            font-size: 16px;
            color: var(--text-dim);
            text-align: center;
            margin-bottom: 40px;
            font-style: italic;
        }

        /* Section */
        .section {
            background: var(--dark);
            border: 1px solid var(--border);
            border-left: 4px solid var(--accent);
            padding: 30px;
            margin-bottom: 30px;
            border-radius: 4px;
        }

        .section-title {
            font-size: 24px;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: 20px;
        }

        .section-subtitle {
            font-size: 18px;
            font-weight: 600;
            color: var(--accent);
            margin-bottom: 15px;
            margin-top: 25px;
        }

        /* Form Elements */
        .form-group {
            margin-bottom: 25px;
        }

        .form-label {
            display: block;
            font-size: 14px;
            font-weight: 600;
            color: var(--text);
            margin-bottom: 10px;
        }

        .form-input,
        .form-textarea {
            width: 100%;
            padding: 14px;
            background: var(--input-bg);
            border: 1px solid var(--border);
            color: var(--text);
            font-size: 15px;
            font-family: 'DM Sans', sans-serif;
            border-radius: 4px;
            transition: all 0.3s ease;
        }

        .form-input:focus,
        .form-textarea:focus {
            outline: none;
            border-color: var(--accent);
            box-shadow: 0 0 0 3px rgba(30, 144, 255, 0.1);
        }

        .form-textarea {
            min-height: 120px;
            resize: vertical;
            line-height: 1.6;
        }

        .form-textarea.large {
            min-height: 180px;
        }

        /* Numbered List */
        .numbered-list {
            counter-reset: item;
            list-style: none;
            padding: 0;
        }

        .numbered-list .form-group {
            counter-increment: item;
            position: relative;
            padding-left: 45px;
        }

        .numbered-list .form-group::before {
            content: counter(item);
            position: absolute;
            left: 0;
            top: 14px;
            width: 32px;
            height: 32px;
            background: var(--accent);
            color: var(--dark);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 14px;
        }

        /* Example Box */
        .example-box {
            background: rgba(30, 144, 255, 0.1);
            border-left: 3px solid var(--accent);
            padding: 15px 20px;
            margin: 15px 0;
            border-radius: 4px;
        }

        .example-box .example-label {
            font-weight: 700;
            color: var(--accent);
            margin-bottom: 5px;
        }

        .example-box p {
            margin: 0;
            font-size: 14px;
            color: var(--text-dim);
        }

        /* Tips Box */
        .tips-box {
            background: rgba(255, 215, 0, 0.08);
            border: 1px solid var(--primary);
            padding: 20px;
            margin: 20px 0;
            border-radius: 4px;
        }

        .tips-box h4 {
            color: var(--primary);
            margin-bottom: 12px;
            font-size: 16px;
        }

        .tips-box ul {
            margin-left: 20px;
            color: var(--text-dim);
        }

        .tips-box ul li {
            margin-bottom: 8px;
            font-size: 14px;
        }

        /* Quote */
        .quote {
            font-size: 18px;
            font-style: italic;
            color: var(--text-dim);
            border-left: 4px solid var(--primary);
            padding-left: 20px;
            margin: 30px 0;
        }

        .quote-author {
            font-size: 14px;
            color: var(--accent);
            margin-top: 10px;
            font-weight: 600;
        }

        /* Differentiation Grid */
        .differentiation-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin: 20px 0;
        }

        .differentiation-item {
            background: var(--input-bg);
            padding: 15px;
            border: 1px solid var(--border);
            border-radius: 4px;
            font-size: 14px;
        }

        /* Print Styles */
        @media print {
            body {
                background: white;
                color: black;
            }

            .container {
                max-width: 100%;
                padding: 20px;
            }

            .action-buttons,
            .no-print {
                display: none !important;
            }

            .header {
                border-bottom: 2px solid #FFD700;
            }

            .section {
                background: white;
                border: 1px solid #ccc;
                page-break-inside: avoid;
                margin-bottom: 20px;
            }

            .form-input,
            .form-textarea {
                border: 1px solid #ccc;
                background: white;
                color: black;
            }

            .main-title {
                color: #0A0E1A;
            }

            .section-title {
                color: #0A0E1A;
            }

            .section-subtitle {
                color: #1E90FF;
            }

            .example-box {
                background: #f0f8ff;
                border-left: 3px solid #1E90FF;
            }

            .tips-box {
                background: #fffef0;
                border: 1px solid #FFD700;
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                align-items: flex-start;
            }

            .action-buttons {
                margin-top: 20px;
                width: 100%;
                flex-direction: column;
            }

            .btn {
                width: 100%;
            }

            .main-title {
                font-size: 28px;
            }

            .differentiation-grid {
                grid-template-columns: 1fr;
            }

            .numbered-list .form-group {
                padding-left: 40px;
            }
        }

        /* Save indicator */
        .save-indicator {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: var(--accent);
            color: var(--dark);
            padding: 12px 20px;
            border-radius: 4px;
            font-size: 14px;
            font-weight: 600;
            opacity: 0;
            transition: opacity 0.3s ease;
            z-index: 1000;
        }

        .save-indicator.show {
            opacity: 1;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo-section">
                <div class="logo">DIGERATI EXPERTS</div>
                <div class="tagline">USP Development Worksheet</div>
            </div>
            <div class="action-buttons no-print">
                <button class="btn" onclick="saveToLocalStorage()">💾 Save Progress</button>
                <button class="btn" onclick="clearForm()">🗑️ Clear Form</button>
                <button class="btn btn-primary" onclick="window.print()">📄 Print to PDF</button>
            </div>
        </div>

        <h1 class="main-title">Developing A Clear, Concise And Compelling Marketing Message</h1>
        <p class="subtitle">"No one knows how good you are until AFTER the sale; before they buy, they only know how good your MARKETING is." – Robin Robins</p>

        <!-- 10 Ways to Differentiate -->
        <div class="section">
            <h2 class="section-title">10 Ways To Differentiate</h2>
            <div class="differentiation-grid">
                <div class="differentiation-item">1. Niche/Specialization</div>
                <div class="differentiation-item">2. Price/Value</div>
                <div class="differentiation-item">3. Guarantee of service level</div>
                <div class="differentiation-item">4. Solving a big problem</div>
                <div class="differentiation-item">5. Guarantee of business result</div>
                <div class="differentiation-item">6. Convenience & ease</div>
                <div class="differentiation-item">7. Proof & validation</div>
                <div class="differentiation-item">8. Affinity (being FOR someone)</div>
                <div class="differentiation-item">9. Marketing & sales process</div>
                <div class="differentiation-item">10. Celebrity (self or clients)</div>
            </div>
        </div>

        <!-- Building Your Case -->
        <div class="section">
            <h2 class="section-title">Building Your Case</h2>
            <p style="margin-bottom: 25px;">Work through this framework to develop a compelling USP for a specific problem or desire your prospects have:</p>

            <div class="form-group">
                <label class="form-label">1. State a problem a typical prospect would have OR something they're LOOKING for:</label>
                <div class="example-box">
                    <div class="example-label">Example:</div>
                    <p>"Most IT firms are highly unresponsive and require us to constantly follow up with them to resolve problems we have."</p>
                </div>
                <textarea class="form-textarea" id="problem1" placeholder="Enter the problem or desire..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">2. Make a claim about how you resolve this problem better than the competition:</label>
                <div class="example-box">
                    <div class="example-label">Example:</div>
                    <p>"We are more responsive than the average IT support firm."</p>
                </div>
                <textarea class="form-textarea" id="claim1" placeholder="Enter your claim..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">3. What is the industry standard?</label>
                <div class="example-box">
                    <div class="example-label">Example:</div>
                    <p>"The average industry response time is 4-6 hours or longer. Most don't guarantee any response time in their SLA and many outsource their help desk."</p>
                </div>
                <textarea class="form-textarea" id="standard1" placeholder="Describe the industry standard..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">4. How do you do it better, SPECIFICALLY? (Detail 2-3 ways)</label>
                <div class="example-box">
                    <div class="example-label">Example:</div>
                    <p>"We have a far more mature and well-staffed help desk than most IT service companies, which enables us to answer our support lines LIVE and guarantee to have a competent technician working on resolving a problem within 10 minutes or less of receiving the call."</p>
                </div>
                <textarea class="form-textarea large" id="better1" placeholder="Detail specifically how you do it better..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">5. Why should the prospect care? What's the specific, QUANTIFIABLE benefit?</label>
                <div class="example-box">
                    <div class="example-label">Example:</div>
                    <p>"If your company has an average of one support request a week and it takes your technician 4 hours to respond on average, you'll end up wasting 208 hours – or 8.6 weeks – WAITING on your technician to simply respond. Is that really acceptable to you?"</p>
                </div>
                <textarea class="form-textarea large" id="benefit1" placeholder="Quantify the benefit to the prospect..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">6. How can you PROVE you can do it better? What evidence can you provide?</label>
                <div class="example-box">
                    <div class="example-label">Example:</div>
                    <p>• Provide testimonials from clients validating this claim<br>• Have a solid guarantee to deliver on this promise<br>• Publish reports showing our average response time was 4.7 minutes over the last 6 months</p>
                </div>
                <textarea class="form-textarea large" id="proof1" placeholder="List your proof points..."></textarea>
            </div>

            <div class="tips-box">
                <h4>💡 Tips for Providing Meaningful Proof:</h4>
                <ul>
                    <li>Actual stats and data from your PSA/RMM tools</li>
                    <li>Bold guarantees with real consequences if you don't deliver</li>
                    <li>Testimonials, case studies, number of reviews</li>
                    <li>Awards won, certifications earned</li>
                    <li>Proof of your systems and documentation</li>
                    <li>Specific details in your claims (e.g., "24 hours of customer service training annually")</li>
                </ul>
            </div>
        </div>

        <!-- Additional USP Development -->
        <div class="section">
            <h2 class="section-title">Additional USP Development</h2>
            <p style="margin-bottom: 25px;">Complete this exercise for every common frustration your prospects have. Use the same framework above:</p>

            <h3 class="section-subtitle">USP Development #2</h3>
            
            <div class="form-group">
                <label class="form-label">Problem/Desire:</label>
                <textarea class="form-textarea" id="problem2" placeholder="Enter the problem or desire..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Your Claim:</label>
                <textarea class="form-textarea" id="claim2" placeholder="Enter your claim..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Industry Standard:</label>
                <textarea class="form-textarea" id="standard2" placeholder="Describe the industry standard..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">How You Do It Better (Specifically):</label>
                <textarea class="form-textarea large" id="better2" placeholder="Detail specifically how you do it better..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Quantifiable Benefit:</label>
                <textarea class="form-textarea" id="benefit2" placeholder="Quantify the benefit..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Proof/Evidence:</label>
                <textarea class="form-textarea" id="proof2" placeholder="List your proof points..."></textarea>
            </div>

            <h3 class="section-subtitle">USP Development #3</h3>
            
            <div class="form-group">
                <label class="form-label">Problem/Desire:</label>
                <textarea class="form-textarea" id="problem3" placeholder="Enter the problem or desire..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Your Claim:</label>
                <textarea class="form-textarea" id="claim3" placeholder="Enter your claim..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Industry Standard:</label>
                <textarea class="form-textarea" id="standard3" placeholder="Describe the industry standard..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">How You Do It Better (Specifically):</label>
                <textarea class="form-textarea large" id="better3" placeholder="Detail specifically how you do it better..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Quantifiable Benefit:</label>
                <textarea class="form-textarea" id="benefit3" placeholder="Quantify the benefit..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Proof/Evidence:</label>
                <textarea class="form-textarea" id="proof3" placeholder="List your proof points..."></textarea>
            </div>
        </div>

        <!-- Top 10 Reasons -->
        <div class="section">
            <h2 class="section-title">Top 10 Reasons Why A Prospect Should Choose You</h2>
            <p style="margin-bottom: 25px;">Summarize your key differentiators into 10 concise, compelling reasons:</p>

            <div class="numbered-list">
                <div class="form-group">
                    <input type="text" class="form-input" id="reason1" placeholder="Reason #1...">
                </div>
                <div class="form-group">
                    <input type="text" class="form-input" id="reason2" placeholder="Reason #2...">
                </div>
                <div class="form-group">
                    <input type="text" class="form-input" id="reason3" placeholder="Reason #3...">
                </div>
                <div class="form-group">
                    <input type="text" class="form-input" id="reason4" placeholder="Reason #4...">
                </div>
                <div class="form-group">
                    <input type="text" class="form-input" id="reason5" placeholder="Reason #5...">
                </div>
                <div class="form-group">
                    <input type="text" class="form-input" id="reason6" placeholder="Reason #6...">
                </div>
                <div class="form-group">
                    <input type="text" class="form-input" id="reason7" placeholder="Reason #7...">
                </div>
                <div class="form-group">
                    <input type="text" class="form-input" id="reason8" placeholder="Reason #8...">
                </div>
                <div class="form-group">
                    <input type="text" class="form-input" id="reason9" placeholder="Reason #9...">
                </div>
                <div class="form-group">
                    <input type="text" class="form-input" id="reason10" placeholder="Reason #10...">
                </div>
            </div>
        </div>

        <!-- Competitive Research -->
        <div class="section">
            <h2 class="section-title">Competitive Research Notes</h2>
            <p style="margin-bottom: 25px;">Use this section to document your competitive analysis:</p>

            <div class="form-group">
                <label class="form-label">Competitor Analysis (Call their office, review their website, secret shop, etc.):</label>
                <textarea class="form-textarea large" id="competitive" placeholder="Document what you learn about competitors..."></textarea>
            </div>

            <div class="tips-box">
                <h4>🔍 Competitive Analysis Checklist:</h4>
                <ul>
                    <li>Call their office - how do they answer?</li>
                    <li>Does their website detail a core value proposition?</li>
                    <li>Submit a contact form - how quickly do they follow up?</li>
                    <li>Do they have testimonials and case studies?</li>
                    <li>Do they offer a guarantee?</li>
                    <li>What do they offer that you don't?</li>
                    <li>Do they have a clear offer on their website?</li>
                    <li>Ask new clients to share competitor proposals</li>
                </ul>
            </div>
        </div>

        <!-- Where to Use Your USP -->
        <div class="section">
            <h2 class="section-title">Where To Showcase Your USP</h2>
            <p style="margin-bottom: 20px;">Document where you'll implement your USP messaging:</p>

            <div class="form-group">
                <label class="form-label">Website Implementation Plan:</label>
                <textarea class="form-textarea" id="website-plan" placeholder="Home page, About Us, dedicated USP page, etc."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Sales Materials Plan:</label>
                <textarea class="form-textarea" id="sales-plan" placeholder="Shock-and-awe materials, proposals, presentations, etc."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Marketing Collateral Plan:</label>
                <textarea class="form-textarea" id="marketing-plan" placeholder="Business cards, brochures, email signatures, etc."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Digital Presence Plan:</label>
                <textarea class="form-textarea" id="digital-plan" placeholder="LinkedIn, Facebook, YouTube videos, etc."></textarea>
            </div>
        </div>

        <!-- Action Items -->
        <div class="section">
            <h2 class="section-title">Action Items & Next Steps</h2>
            
            <div class="form-group">
                <label class="form-label">Immediate Action Items:</label>
                <textarea class="form-textarea large" id="actions" placeholder="List specific tasks to implement your USP..."></textarea>
            </div>

            <div class="form-group">
                <label class="form-label">Person Responsible / Due Date:</label>
                <textarea class="form-textarea" id="responsible" placeholder="Assign ownership and deadlines..."></textarea>
            </div>
        </div>

        <div class="no-print" style="text-align: center; margin: 40px 0; padding: 30px; background: var(--dark); border-radius: 4px;">
            <p style="margin-bottom: 20px; color: var(--text-dim);">Ready to print your worksheet?</p>
            <button class="btn btn-primary" onclick="window.print()" style="font-size: 16px; padding: 16px 32px;">
                📄 Print to PDF
            </button>
            <p style="margin-top: 15px; font-size: 14px; color: var(--text-dim);">
                Tip: In the print dialog, select "Save as PDF" as your printer
            </p>
        </div>

    </div>

    <div class="save-indicator" id="saveIndicator">✓ Progress Saved</div>

    <script>
        // Auto-save to localStorage
        const formFields = [
            'problem1', 'claim1', 'standard1', 'better1', 'benefit1', 'proof1',
            'problem2', 'claim2', 'standard2', 'better2', 'benefit2', 'proof2',
            'problem3', 'claim3', 'standard3', 'better3', 'benefit3', 'proof3',
            'reason1', 'reason2', 'reason3', 'reason4', 'reason5', 
            'reason6', 'reason7', 'reason8', 'reason9', 'reason10',
            'competitive', 'website-plan', 'sales-plan', 'marketing-plan', 'digital-plan',
            'actions', 'responsible'
        ];

        // Load saved data on page load
        window.addEventListener('load', function() {
            formFields.forEach(fieldId => {
                const savedValue = localStorage.getItem('usp_' + fieldId);
                if (savedValue) {
                    const field = document.getElementById(fieldId);
                    if (field) {
                        field.value = savedValue;
                    }
                }
            });
        });

        // Auto-save on input
        formFields.forEach(fieldId => {
            const field = document.getElementById(fieldId);
            if (field) {
                field.addEventListener('input', function() {
                    localStorage.setItem('usp_' + fieldId, this.value);
                    showSaveIndicator();
                });
            }
        });

        function saveToLocalStorage() {
            formFields.forEach(fieldId => {
                const field = document.getElementById(fieldId);
                if (field) {
                    localStorage.setItem('usp_' + fieldId, field.value);
                }
            });
            showSaveIndicator();
        }

        function showSaveIndicator() {
            const indicator = document.getElementById('saveIndicator');
            indicator.classList.add('show');
            setTimeout(() => {
                indicator.classList.remove('show');
            }, 2000);
        }

        function clearForm() {
            if (confirm('Are you sure you want to clear all form data? This cannot be undone.')) {
                formFields.forEach(fieldId => {
                    const field = document.getElementById(fieldId);
                    if (field) {
                        field.value = '';
                        localStorage.removeItem('usp_' + fieldId);
                    }
                });
                alert('Form cleared successfully!');
            }
        }

        // Print setup
        window.addEventListener('beforeprint', function() {
            document.title = 'USP Worksheet - Digerati Experts - ' + new Date().toLocaleDateString();
        });
    </script>
</body>
</html>