Office page: what Replit needs to fix
What’s working (keep)

The positioning is clear: “small offices (5–25 users) with no internal IT” and “one predictable monthly price.” 

Office

The six Key Features are the right buckets (Helpdesk, Security baseline, IAM, Maintenance, Backup, Documentation). 

Office

What’s currently hurting the sales pitch

Mixed product naming

Page headline says “ProActive Ecosystem Packages” but the content is the Office package. This dilutes the offer. 

Office

Pricing model ambiguity

The page shows $825/mo and also $165/user (5-user minimum). That’s fine, but it must be consistently presented everywhere on the page (and match your current official Office pricing). 

Office

The “Service Tiers” matrix is dense and partially mismatched

It mixes “Managed IT”, “Cybersecurity”, “Cloud & Backup”, “Identity & Access” into one long grid with ambiguous checkmarks and “optional” chips—hard to scan, and it creates scope questions instead of reducing them. 

Office

The same page also has a “Recommended plan” card that lists inclusions (Email/Calendar/Team Chat, MFA/SSO/Password Manager, EDR, Anti-Phishing, Managed Network/Internet, Service Desk + Backup Strategy) that should map perfectly to matrix rows—but currently it’s not 1:1 obvious. 

Office

Replit rebuild plan for the Office page (structure + UI)
1) Page goal

Convert Office intent into Schedule Consultation (primary) or Get Pricing (secondary) with minimal confusion.

2) New section order (Office-specific)

Hero: “Office” offer (not “packages”)

Who it’s for + outcomes (3–5 bullets)

What’s included (deliverables) (the “what you get” section, tightened)

Office package card (pricing + 6 inclusions + 3 exclusions)

Compare tiers (short compare) + “View full matrix” link

FAQs

Final CTA

3) UI changes

Add a sticky CTA (“Schedule Consultation” + “Get Pricing”).

Replace the mega-matrix default view with:

a Short Compare (≤10 rows)

then a Full Matrix behind a toggle (filters by category, collapsible groups)

Add a “What’s not included in Office” accordion to prevent scope creep (examples: MDR/SOC, continuous vuln management, DRaaS, PAM, advanced compliance reporting).

Office service matrix: make it up-to-date and internally consistent

Below is a clean “source of truth” matrix spec that Replit should implement so the matrix is always “properly up to date”:

A) Matrix rules (non-negotiable)

Every bullet on the Office “Recommended Plan” card must map to an explicit row in the matrix.
Current card bullets that must map:

Email + Calendar + Team Chat

MFA + SSO + Password Manager

Endpoint Security (EDR)

Email Protection (Anti-Phishing)

Managed Network + Internet

Service Desk + Backup Strategy 

Office

Every row must be one of:

Included

Included (limited) (define the limit in tooltip)

Optional Add-On

Not included

The matrix must support tier-specific detail (not just checkmarks):

SLA hours (ex: 8x5 vs priority)

cadence (monthly/quarterly/continuous)

depth (baseline vs advanced vs zero trust)

B) Updated matrix content (Office / Business / Enterprise)

Use these groups (matches what your page is already trying to show): Managed IT, Cybersecurity, Cloud & Backup, Identity & Access. 

Office

1) Managed IT

Help Desk & SLA (Office = 8x5)

Remote Monitoring & Alerting

Patch & App Management

Remote Support & Quick Assist

Client Portal & Ticketing

IT Documentation & Knowledge Base (Office = Standard)

Asset & Warranty tracking (Office = Basic)

Executive Reporting / QBRs (Office = Semi-annual)

vCIO (Office = Semi-annual)

This is already on your matrix—Replit needs to standardize labels and ensure Office values are explicit (not implied by a dot/check). 

Office

2) Cybersecurity

Keep the intent, but tighten labels so buyers understand:

Baseline Threat Protection

Data Safeguards & Identity Controls

Email Security & MFA (Office = “Secure gateway”)

Endpoint Detection & Response (Office = “EDR”)

Security Awareness & Phishing (Office = “Baseline”)

SaaS App Security Monitoring (Office = Optional)

Dark Web Monitoring

DNS Filtering & Web Gateway

Vulnerability Scanning (Office = Quarterly)

Incident Response Plan (Office = Template)

These items appear in your current grid; make the “baseline vs advanced” meaning visible with tooltips. 

Office

3) Cloud & Backup

These should be crystal-clear because “Backup Strategy” is on the Office card:

Endpoint Backup (Office = Included)

SaaS Backup (M365/Google) (Office = define included/optional)

Server/VM Backup (Office = Optional)

Immutable Backup Copies (Office = Optional or Included—pick one and make it consistent)

Verified Restore Testing (Office = Quarterly)

Disaster Recovery (DRaaS) (Office = Optional)

RTO/RPO Guarantees (Office = Not included / “Standard” if you truly provide them)

Your matrix already lists these; the fix is consistency + definitions. 

Office

4) Identity & Access

These are on page 3 and need to align to your “MFA + SSO + Password Manager” promise:

Cloud Directory & SSO (Office = Included)

Multi-Factor Authentication (Office = Included)

Conditional Access Policies (Office = Basic)

User Lifecycle Automation (Office = Not included or Optional—pick one)

Privileged Access Management (Office = Optional / Not included)

Access Reviews & Auditing (Office = Quarterly / Not included—pick one)

Again: these exist in your current matrix; Replit’s job is to make them unambiguous and mapped to the Office promise. 

Office

C) Short Compare (what should show above the full matrix)

Replit should render a 10-row compare that answers buying questions:

Support hours (SLA)

Patch + monitoring coverage

Email security level

Endpoint protection level (EDR vs MDR)

Backup type + restore testing cadence

Vulnerability scanning cadence

Conditional access maturity

Documentation depth + client visibility

Reporting cadence (QBR / vCIO)

Optional add-ons available (DRaaS, PAM, etc.)

Then: “View full service matrix” expands the detailed grid. 

Office

D) Implementation requirements for Replit (so it stays “up-to-date”)

Store tiers + rows in a single JSON config and render:

groups[] -> rows[] -> tierValues

Add a “last updated” version tag inside that config.

Enforce a validation step:

every “Recommended Plan” bullet must match a row key (no orphaned marketing claims).