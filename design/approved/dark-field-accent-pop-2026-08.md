# Dark field + accent pop — Aug 2026

Mechanism (not an OpenMSP clone): near-black wells, charcoal raised boxes with hairline borders, white type, brand magenta `#D3126A` as punctuation (CTA, active chip, colon). Color pops because the field stays unsaturated.

Do not copy OpenMSP/OpenFrame product UI, logos, or “Featured in” chrome.

Rule: `.cursor/rules/dark-field-accent-pop.mdc`. Tokens: `DESIGN_SYSTEM.md`.
