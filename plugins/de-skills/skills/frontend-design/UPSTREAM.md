# Vendored frontend-design skill

- Source: "Nate's Frontend Design" skill pack, supplied by Joe as an upload on 2026-09-02
  (`SKILL.md` only; the `LICENSE.txt` its frontmatter references was not included in the upload).
- Installed for: Claude Code project skill discovery (`/frontend-design`).
- Audited: 2026-09-02

## Local deviations from the upload

One blockquote was inserted directly after the frontmatter, labelled as a local
addition. It states that on `client/` production surfaces the locked DE brand
foundation (`AGENTS.md`, `.cursorrules` section 9A, `design/UI-STYLE-RULES.md`)
takes precedence over the skill's "avoid Inter / Space Grotesk / invent a palette"
guidance, which still applies to isolated experiments. No other text was changed.

## External-service boundary

None. The skill is guidance only and makes no network calls.
